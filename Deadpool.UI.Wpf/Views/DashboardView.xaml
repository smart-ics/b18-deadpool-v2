<UserControl x:Class="Deadpool.UI.Wpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:Deadpool.UI.Wpf.Controls"
             mc:Ignorable="d"
             d:DesignWidth="1280"
             d:DesignHeight="720"
             Background="{StaticResource BackgroundBrush}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="110" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Style="{StaticResource HeaderBorderStyle}"
                Margin="0,0,0,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="{StaticResource RedBrush}"
                           Text="CRITICAL - STORAGE FAILURE IMMINENT" />

                <TextBlock Grid.Row="1"
                           Margin="0,4,0,0"
                           FontSize="11"
                           Foreground="{StaticResource MutedTextBrush}"
                           Text="Last update: 10:25" />
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <controls:StatusCard Grid.Column="0"
                                 Title="Last Backup"
                                 StatusText="FULL / DIFF / LOG"
                                 DetailText="FULL ✔ 10:25   DIFF ✔ 10:26   LOG ✔ 10:26"
                                 StatusColor="{StaticResource GreenBrush}" />

            <controls:StatusCard Grid.Column="2"
                                 Title="Chain Health"
                                 StatusText="CRITICAL"
                                 DetailText="Restore chain broken"
                                 StatusColor="{StaticResource RedBrush}" />

            <controls:StatusCard Grid.Column="4"
                                 Title="Storage"
                                 StatusText="WILL FAIL"
                                 DetailText="Free: 15GB&#x0a;Next: 45GB ❌&#x0a;Exhaustion: ~2h"
                                 StatusColor="{StaticResource YellowBrush}" />

            <controls:StatusCard Grid.Column="6"
                                 Title="Database"
                                 StatusText="ONLINE"
                                 DetailText="Instance: PROD-SQL-01"
                                 StatusColor="{StaticResource GreenBrush}" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Style="{StaticResource CardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Margin="0,0,0,8"
                               Style="{StaticResource PanelHeadingTextStyle}"
                               Text="Recent Jobs" />

                    <DataGrid Grid.Row="1"
                              Style="{StaticResource DashboardDataGridStyle}"
                              ItemsSource="{Binding RecentJobs}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*" />
                            <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="*" />
                            <DataGridTemplateColumn Header="Status" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock FontSize="11"
                                                   FontWeight="SemiBold"
                                                   Text="{Binding Status}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="SUCCESS">
                                                            <Setter Property="Foreground" Value="{StaticResource GreenBrush}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="FAILED">
                                                            <Setter Property="Foreground" Value="{StaticResource RedBrush}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="PENDING">
                                                            <Setter Property="Foreground" Value="{StaticResource YellowBrush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="2"
                    Style="{StaticResource CardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Margin="0,0,0,8"
                               Style="{StaticResource PanelHeadingTextStyle}"
                               Text="Alerts" />

                    <StackPanel Grid.Row="1">
                        <Border Style="{StaticResource AlertItemBorderStyle}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource AlertMessageTextStyle}" Foreground="{StaticResource RedBrush}" Text="❌ No FULL backup" />
                                <TextBlock Margin="0,3,0,0" Style="{StaticResource AlertActionTextStyle}" Text="👉 RUN FULL NOW" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource AlertItemBorderStyle}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource AlertMessageTextStyle}" Foreground="{StaticResource YellowBrush}" Text="⚠ Storage pressure high" />
                                <TextBlock Margin="0,3,0,0" Style="{StaticResource AlertActionTextStyle}" Text="👉 PURGE OLD CHAINS" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource AlertItemBorderStyle}" Margin="0">
                            <StackPanel>
                                <TextBlock Style="{StaticResource AlertMessageTextStyle}" Foreground="{StaticResource RedBrush}" Text="❌ Restore risk" />
                                <TextBlock Margin="0,3,0,0" Style="{StaticResource AlertActionTextStyle}" Text="👉 VERIFY BACKUP ORDER" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
