<Window x:Class="Deadpool.UI.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:Deadpool.UI.Wpf.Views"
        mc:Ignorable="d"
        Title="Deadpool Backup Monitor"
        Width="1280"
        Height="720"
        MinWidth="1280"
        MinHeight="720"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="12,12,12,0" LastChildFill="False">
            <Button Content="Restore..."
                    MinWidth="110"
                    Padding="10,4"
                    Click="OnOpenRestoreDialogClick" />
        </DockPanel>

        <views:DashboardView Grid.Row="1" />
    </Grid>
</Window>
